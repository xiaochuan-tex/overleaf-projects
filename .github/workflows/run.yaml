name: Job
permissions: write-all
on:
  schedule:
    - cron: '0 0,6,12,18 * * *'
  workflow_dispatch:
  push:

jobs:
  run-daily:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: clone source
        run: |
          curl -H "Cookie: ${{ secrets.COOKIE }}" "https://www.overleaf.com/project/6858df18adf054946faa2f55/download/zip"  -o source1.zip
          ls -R
          unzip ./source1.zip -d ./教育知识与能力

      - name: Cache downloaded file
        uses: actions/cache@v4
        id: cache-software
        with:
          path: /usr/local/texlive/2025
          key: ${{ runner.os }}-texlive2025

      - name: install texlive
        if: steps.cache-software.outputs.cache-hit != 'true'
        run: |
          cd /tmp
          curl -L -o install-tl-unx.tar.gz https://mirrors.ustc.edu.cn/CTAN/systems/texlive/tlnet/install-tl-unx.tar.gz
          tar -xzf install-tl-unx.tar.gz
          cd install-tl-2*
          sudo ./install-tl --scheme=full --no-interaction

      # - name: install font
      #   run: | 
      #     apt update
      #     apt install fonts-noto-cjk

      # - name: compile
      #   run: |
      #     export PATH=$PATH:/usr/local/texlive/2025/bin/x86_64-linux
      #     cd ./教育知识与能力
      #     xelatex -jobname=教育知识与能力 main.tex
      #     xelatex -jobname=教育知识与能力 main.tex
      #     cp ./*.pdf ../

      # - name: commit
      #   run: |
      #     git config --global user.name "GitHub Actions"
      #     git config --global user.email "actions@github.com"
      #     git add ./*.pdf
      #     if [ -n "$(git status --porcelain)" ]; then
      #       git commit -m 'update'
      #       git push
      #     else
      #       echo "No changes to commit"
      #     fi